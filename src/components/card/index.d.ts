type TCardKeys = 'elevate' | 'airshow' | 'regatta' | 'silverstone' | '"wimbledon' | 'lorem';

type TCard = {
  key: TCardKeys,
  alt: string,
  title: string,
  src: string,
  detailFirst: string,
  detailSecond: string,
}